import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  featureItem: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 20,
  },
  featureText: {
    color: "#E0E0FF",
    fontSize: 18,
    fontWeight: "500",
  },
});
